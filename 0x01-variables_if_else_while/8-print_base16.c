#include <stdio.h>
/**
 * main - entry
 *
 * Return: 0
 */
int main(void)
{
	int n;
	int nn;

	for (n = 48 ; n <= 57 ; n++)
	{
		putchar(n);
	}
	for (nn = 97 ; n <= 102 ; nn++)
	{
		putchar(nn);
	}
	putchar('\n');
	return (0);
}
